{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Schema for 1:1 Anforderung-to-Kontrolle Mapping",
  "description": "Defines a structure for mapping requirements (ED2023-like IDs) to controls (GPP-like IDs) and listing unmapped items.",
  "type": "object",
  "properties": {
    "mapping": {
      "type": "object",
      "description": "A 1:1 map where keys are ED2023 requirements and values are GPP controls.",
      "additionalProperties": {
        "type": "string",
        "description": "Any valid control or requirement ID."
      }
    },
    "unmapped_gpp": {
      "type": "array",
      "description": "List of GPP control IDs that were not mapped to any ED2023 requirement.",
      "items": {
        "type": "string",
        "pattern": "^[A-Z]{2,}(\\.\\d+)+(?:\\.A\\d+)?$"
      }
    },
    "unmapped_ed2023": {
      "type": "array",
      "description": "List of ED2023 requirement IDs that were not mapped to any GPP control.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["mapping", "unmapped_gpp", "unmapped_ed2023"]
}